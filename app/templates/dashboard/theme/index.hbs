{{title "Инсталирани теми"}}
{{nav-bar}}
<div class="container">
  <div class="page-title">
    <h2 class="float-xs-left">Инсталирани теми</h2>
    {{#link-to "dashboard.theme.store" class="btn btn-success float-xs-right"}}
      {{fa-icon "shopping-cart"}} Магазин
    {{/link-to}}
  </div>
  {{#if haveThemes}}
    <div class="row">
      {{#each themes as |theme|}}
        <div class="col-xl-4 col-md-6">
          <div class="card card-block">
            <div class="card-title">
              <h4 title="{{theme.name}}">{{theme.name}}</h4>
              <button class="btn btn-{{if theme.active 'success' 'secondary'}}" {{action "select" theme}}>
                {{#if theme.active}}
                  Приложена
                {{else}}
                  Приложи
                {{/if}}
              </button>
            </div>
            <div class="card-img">
              <img class="img-fluid m-x-auto d-block" src="{{theme.cover}}" alt="">
            </div>
            {{!-- {{#link-to "dashboard.theme.index" class="card-link"}}Персонализирай{{/link-to}} --}}
            <a href="{{theme.demo_url}}" class="card-link" target="_blank">Демо</a>
            {{#if canUninstall}}
              <button class="btn btn-link card-link" {{action "remove" theme}} disabled={{disableRemove}}>Деинсталирай</button>
            {{/if}}
          </div>
        </div>
      {{/each}}
    </div>
  {{else}}
    <div class="alert alert-info" role="alert">
      Няма инсталирани теми. Можете да инсталирате от страницата Магазин за теми.
    </div>
  {{/if}}
</div>

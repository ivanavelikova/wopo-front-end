<div class="center-wrapper center-wrapper--auth">
  <section class="center-content center-content--auth">
    {{#link-to "index" class="auth-logo"}}
      {{logo-symbol class="logo-symbol"}}
      {{logo-text class="logo-text"}}
    {{/link-to}}
    <form {{action "register" on="submit"}} class="auth-form">
      <h2 class="text-xs-center">Регистрация</h2>
      
      {{#if showSuccessAlert}}
        <div class="alert alert-success" role="alert">
          {{alerts.success}}
        </div>
      {{/if}}
      
      {{#if showDangerAlert}}
        <div class="alert alert-danger" role="alert">
          {{alerts.danger}}
        </div>
      {{/if}}

      {{validated-input model=model
        label="Име"
        value=name
        valuePath="name"
        type="text"
        placeholder="Напиши името си"
        autocomplete="off"
        disabled=disableForm
      }}
      {{validated-input model=model
        label="E-mail"
        value=email
        valuePath="email"
        type="email"
        placeholder='Напиши е-mail адреса си'
        autocomplete="off"
        disabled=disableForm
      }}
      {{validated-input model=model
        label="Парола"
        value=password
        valuePath='password'
        type="password"
        placeholder='Напиши паролата си'
        disabled=disableForm
      }}
      <p class="terms text-xs-center">
        С регистрацията Вие се съгласявате с нашите {{#link-to "index"}}Условия на ползване{{/link-to}}.
      </p>
      <button role="submit" class="btn btn-success btn-block" disabled={{disableForm}}>Регистрация</button>
    </form>
    <div class="auth-bottom"> 
      <p>Вече имате профил? {{#link-to "login"}}Вход{{/link-to}}</p>
    </div>
  </section>
</div>

<div class="col-xl-4 col-md-6">
  <div class="card card-block">
    <div class="card-title">
      <h4 title="{{data.name}}" class="text-xs-center">{{data.name}}</h4>
    </div>
    <button class="btn btn-link card-link" data-toggle="modal" data-target=".modal.editSkill" {{action edit index}}>Редактирай</button>
    <button class="btn btn-link card-link" {{action delete index}}>Изтрий</button>
  </div>
</div>
